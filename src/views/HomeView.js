import React, { Node } from "react";
import {Text, View} from 'react-native';

const HomeView = (): Node => {
    return (
        <View>
            <Text>test</Text>
        </View>
    )
}

export default HomeView;
